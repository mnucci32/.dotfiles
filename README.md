# .dotfiles
Profile configuration files

# Usage

Clone to $HOME dirctory

Use GNU stow to create symlinks for each package:

```zsh
stow zsh
```

Use `.local` files for any machine specific overrides